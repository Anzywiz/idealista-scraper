# Copy this file to .env and fill in your rotating proxy URL.
# A single URL is enough — curl_cffi and every browser pool slot will both
# route through it. Leave it unset / delete the line to run without a proxy.
PROXY_URL=your_value_here
